# Build artifacts
/target/
/tla/states/
/tla/.tools/
README.pdf
*.pdf

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

*.bin
# …but keep the cosign-verification test fixtures (a tiny signed test blob).
!tests/fixtures/cosign/*.bin
tla/*_TTrace_*.bin
tla/*_TTrace_*.tla

# OS
.DS_Store
Thumbs.db

# Claude Code local state
.claude/

# Debug/diagnostic files
*.long-type-*.txt
/testing/artifacts/
/testing/two_sync_capture.txt
/testing/two_sync_metrics_capture.txt
__pycache__/
.mypy_cache/
.ruff_cache/
.venv/

# Environment
.env
.env.local

# proptest-regressions/ is deliberately NOT ignored: proptest writes the
# minimized counterexample there when a property fails, and committing it
# replays that exact case forever. Ignoring it would discard a found bug.

# Elle build outputs
testing/third_party/elle/*.jar
testing/elle_shim/target/
testing/elle_shim/.cpcache/
testing/elle_shim/pom.xml
testing/third_party/elle/.build-stamp
